# Prompts

Prompts live here as versioned `.yaml` or `.txt` files — never as inline strings in
`orchestration/graph.py`. Suggested convention: `<workflow>/<node_name>.v1.yaml` with
`system` / `user` template fields.
