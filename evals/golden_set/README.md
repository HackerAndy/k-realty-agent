# Golden set

Historical Property Finance Tracker Agent cases + expected outputs, one file per case (e.g.
`case_0001.json` with `input: Transaction` and `expected: Decision`).
Target: 100 cases before the Phase 1 gate.
